---
title: Get the Length of an Object with JavaScripts Object.keys Method
date: 2020-05-28
slug: javascript-get-the-length-of-an-object-with-javascripts-object-keys-method
image: ./images/cover.png
icon: ../icons/javascript.png
tags: ["javascript"]
lead: In this lesson, we get the length of an Object by using the Object.keys() method.
video: true
---

import EggheadEmbed from "../../../src/components/eggheadEmbed"

<EggheadEmbed
  lessonTitle="Get the Length of an Object with JavaScripts Object.keys Method"
  lessonLink="https://egghead.io/lessons/javascript-get-the-length-of-an-object-with-javascripts-object-keys-method/embed"
/>

Let's start by creating an `Object`.

```jsx
const pokemon = {}
```

This `Object` will contain the following properties: `name`, `type`, `level`.

```jsx
const pokemon = {
  name: "Charmander",
  type: "🔥",
  level: 400,
}
```

We then `console.log` the `Object` and try to get the length of the `Object` by using the `length` property.

```jsx
console.log(pokemon)
console.log(pokemon.length)
```

This returns `undefine`. Instead, we use `Object.keys()` method to return an array of the enumerable property names.

```jsx
console.log(Object.keys(pokemon))
```

Then we determine the length of the array, by using the `length` property on the enumerable property names.

```jsx
const pokemon = {
  name: "Charmander",
  type: "🔥",
  level: 400,
  color: "orange",
}

console.log(Object.keys(pokemon).length)
```
